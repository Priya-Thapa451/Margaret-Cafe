// import express from "express";
// import { getProfile } from "../controller/profileController.js";
// import { tokenVerify } from "../middleware/tokenVerify.js"; // Assuming this middleware exists

// const profileRouter = express.Router();

// // Route to get a user's profile (e.g., Cafe Manager or Staff)
// profileRouter.get("/profile", tokenVerify, getProfile);

// export default profileRouter;
